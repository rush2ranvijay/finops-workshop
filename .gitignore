# Generated personal telemetry — NEVER commit (contains real session costs/usage)
*.db
demos/demo3-meter/dashboard_data.json
demos/demo3-meter/data/
demos/demo3-meter/*.local.json

# Python / env
__pycache__/
*.pyc
.venv/
venv/

# apm — installed dependency tree (created by `apm install`)
apm_modules/

# OS
.DS_Store

# Demo 3 — local-only personal telemetry (regenerate with build_db.py + export_dashboard.py)
demos/demo3-meter/dashboard.html
demos/demo3-meter/EVIDENCE.md
demos/demo3-meter/*_b44.json
demos/demo3-meter/testdata/

# Canvas source of truth lives in .apm/extensions/finops-dashboard/ (shipped via apm).
# `apm install --target copilot` DEPLOYS it here — that copy is a generated artifact.
.github/extensions/finops-dashboard/
# Local telemetry snapshot dropped next to the deployed/dev canvas — never commit.
.apm/extensions/finops-dashboard/dashboard_data.json
